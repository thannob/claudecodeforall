# Task List — แนะนำอาหารไทย

อิงจาก `CONTEXT.md` (คำศัพท์: รายการโปรด / เมนูสุ่มวันนี้) และผลสรุปจากการ grill

## Setup

- [x] เพิ่ม `<script>` แยกส่วนท้าย `index.html` (ก่อน `</body>`) สำหรับ JS ของทั้งสองฟีเจอร์
- [x] ให้แต่ละการ์ดเมนูมี identifier คงที่ (เช่น `data-dish` = ชื่อเมนู) เพื่อใช้อ้างอิงใน JS และ localStorage
- [x] ตั้ง `.card` เป็น `position: relative` เพื่อรองรับไอคอนหัวใจมุมขวาบน

## 🎲 เมนูสุ่มวันนี้ (Random Pick)

- [x] เพิ่มปุ่ม "สุ่มเมนูอาหารไทยวันนี้" และกล่องแบนเนอร์ผลลัพธ์ ใต้ header เหนือตารางเมนู
- [x] กล่องแบนเนอร์แสดงข้อความ placeholder ชวนกด ก่อนกดปุ่มครั้งแรก (เช่น "กดปุ่มเพื่อสุ่มเมนูเด็ดวันนี้!")
- [x] เขียนฟังก์ชันสุ่มเมนูจาก array ของเมนูทั้ง 9 รายการ (รวมของหวาน)
- [x] กันไม่ให้สุ่มซ้ำเมนูเดิมติดกัน โดยเก็บ "เมนูล่าสุด" ไว้ในตัวแปรความจำ (ไม่ใช้ localStorage รีเฟรชแล้วรีเซ็ต)
- [x] อัปเดตข้อความในกล่องแบนเนอร์เป็นเมนูที่สุ่มได้ (อีโมจิ + ชื่อเมนู)
- [x] ใส่ CSS transition แบบ fade-in สั้นๆ (~0.3s) ตอนข้อความในกล่องเปลี่ยน

## ❤️ รายการโปรด (Favorites)

- [x] เพิ่มไอคอนหัวใจ (♡) มุมขวาบนของแต่ละการ์ดเมนู
- [x] คลิกไอคอนแล้ว toggle สถานะถูกใจ (♡ ↔ ❤️) ของเมนูนั้นๆ อิสระต่อกัน (เลือกได้หลายเมนูพร้อมกัน)
- [x] บันทึก/อ่านสถานะรายการโปรดจาก localStorage โดยอ้างอิง identifier ของแต่ละเมนู
- [x] โหลดหน้าใหม่แล้วต้องคืนสถานะไอคอนหัวใจให้ตรงกับที่บันทึกไว้ก่อนหน้า
- [x] ไม่ต้องมี toast/ข้อความแจ้งเตือนเพิ่มเติม — แค่ไอคอนเปลี่ยนสีตอนกดพอ
- [x] ไม่ต้องทำปุ่ม/ตัวกรอง "ดูเฉพาะรายการโปรด" แยกต่างหาก

## Verify

- [ ] ทดสอบกดปุ่มสุ่มหลายครั้งติด ตรวจว่าไม่ออกเมนูเดิมซ้ำสองครั้งติดกัน _(ตรวจ logic ด้วยการอ่านโค้ด + node --check ผ่าน แต่ยังไม่ได้ทดสอบจริงในเบราว์เซอร์ เพราะ Claude in Chrome extension ไม่ได้เชื่อมต่อ)_
- [ ] ทดสอบกดหัวใจหลายใบพร้อมกัน แล้ว refresh หน้า ตรวจว่าสถานะยังอยู่ครบ _(ยังไม่ได้ทดสอบจริงในเบราว์เซอร์ด้วยเหตุผลเดียวกัน)_
- [ ] ทดสอบบนมือถือ (responsive) ว่าไอคอนหัวใจและกล่องแบนเนอร์ไม่ล้นเลย์เอาต์เดิม _(ยังไม่ได้ทดสอบจริง)_

## 🧂 หน้ารายละเอียดส่วนประกอบเมนู (Ingredient Detail Page)

- [x] แยก CSS เดิมออกเป็น `styles.css` ใช้ร่วมกันระหว่าง `index.html` และหน้ารายละเอียดใหม่
- [x] เพิ่ม `menu-data.js` เก็บข้อมูลเต็มของทั้ง 9 เมนู (ชื่อ, อีโมจิ/รูป, คำอธิบาย, แท็ก, หมวดหมู่, ส่วนประกอบ) เป็น single source of truth สำหรับหน้ารายละเอียด
- [x] เพิ่มลิงก์ "ดูส่วนประกอบ →" ในการ์ดแต่ละใบใน `index.html` ลิงก์ไปยัง `detail.html?dish=<ชื่อเมนูที่ encode แล้ว>` (href ถูกตั้งค่าโดย JS จาก `data-dish` ของการ์ด เพื่อไม่ให้ต้องพิมพ์ชื่อเมนูซ้ำสองที่)
- [x] สร้าง `detail.html` อ่านค่า query param `dish`, ค้นหาใน `menu-data.js`, แสดงรูป/อีโมจิ, คำอธิบาย, แท็ก และรายการส่วนประกอบ
- [x] จัดการกรณีไม่พบเมนู (query param ไม่ตรงกับข้อมูลใดๆ) ด้วยข้อความ "ไม่พบเมนูนี้"
- [x] ตรวจสอบด้วย `node --check` ว่า JS ทุกไฟล์ syntax ถูกต้อง และจำลอง logic การค้นหาเมนูด้วย node เทียบชื่อเมนูใน `index.html` กับ `menu-data.js` ว่าตรงกันครบทั้ง 9 รายการ
- [ ] ทดสอบจริงในเบราว์เซอร์ (คลิกลิงก์จากหน้าแรกไปหน้ารายละเอียด, ตรวจ responsive มือถือ) _(ยังไม่ได้ทดสอบจริง เพราะ Claude in Chrome extension ไม่ได้เชื่อมต่อ)_
